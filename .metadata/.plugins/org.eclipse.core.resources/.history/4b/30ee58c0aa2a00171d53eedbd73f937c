package ag.proxyserver;

public class WebCamCodeGenerator {
	char[] hashs = ['A', 'B', 'C', 'F', 'X', 'Z', '0', '3', '8', '9'];//10
	
	
	public static String code(){
		double code = Math.random()*1000000;
		return String.format("COD-%06.0f", code);
		//CAM @@@@@@-##
	}
		
}
