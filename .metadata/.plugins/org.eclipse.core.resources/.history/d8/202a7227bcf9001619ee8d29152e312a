
public class Timer {
	
	public static void main(String[] args) {
		java.util.Timer t = new java.util.Timer();
		//
		t.scheduleAtFixedRate(task, delay, period);//delay 2s e period 5s
		//
		Thread.sleep(delay);
		while()
			exec task (20s)//<-- thread
			Thread.sleep(millis);
	}

}
