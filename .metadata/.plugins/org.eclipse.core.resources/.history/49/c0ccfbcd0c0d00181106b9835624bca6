package ag.threads;

public class BlockingChannel {
	private int id = 0;
	private int state = 0;
	private Object lock = new Object();
	
	public void lock(){
		if (state == 1){
			synchronized (lock) {
				lock.wait();
			}
		} else {
			state = 1;
		}
	}
	
	public void unlock()
	
}
